console.log(parseInt("5") + 1);

/*var occurancesArray = [3,4,5,0,5,6,7,0,2,3];

var dictionary = {};
var tempIndex = 1;
occurancesArray.forEach(function(element){
  dictionary[tempIndex] = element
  tempIndex++;
})

var sortable = [];
for (var vehicle in dictionary) {
    sortable.push([vehicle, dictionary[vehicle]]);
}
sortable.sort(function(a, b) {
    return a[1] - b[1];
});
sortable.reverse();

for (var i = 0; i < sortable.length; i++){
  if(sortable[i][1] == 0){
    delete sortable[i];
  }
}

for(var i = 0; i < sortable.length; i++){
  //TODO REPLACE all keys with corresponding time stamp
}*/


//console.log(JSON.stringify(dictionary));
//console.log(sortable);
//console.log(sortable[0]);

/*
for(var i = 0; i < array.length; i++){
    if(array[i] != 0){
        var counter = i+1;
        while(array[counter] != 0 && counter < array.length){
          counter++;
        }
        if(counter-1 != i){
          //array.fill(0, i+1, counter);
          //console.log(array.fill(0, i+1, counter));
          array = array.fill(0, i+1, counter);
          console.log(array.fill(0, i+1, counter));
        }
        i = counter-1;
    }
}


array.forEach(function(element) {
  console.log(element);
});
*/
